## set up server via nupo

to download nupo into your project directory:
```bash
wget https://raw.githubusercontent.com/johnhewi/nupo/main/nupo
```

to run nupo:
```bash
bash nupo
```

to download nupo.config template:
```bash
wget https://raw.githubusercontent.com/johnhewi/nupo/main/nupo.config
```
